import { FileText, Sparkles } from 'lucide-react';
const examples = ['Explain photosynthesis', 'JavaScript closures', 'French Revolution'];
export function StudyInput({ notes, onNotesChange, onGenerate, loading }) { return <section className="study-input"><div className="input-heading"><span><FileText size={17} /> Study material</span><small>Paste up to 12,000 characters</small></div><textarea value={notes} maxLength={12000} onChange={(e) => onNotesChange(e.target.value)} placeholder="Paste your lecture notes, chapter summary, code concepts, or any topic you want to learn…" /><div className="input-footer"><div className="examples">Try: {examples.map(x => <button key={x} onClick={() => onNotesChange(x)}>{x}</button>)}</div><button className="primary-button generate" disabled={loading || notes.trim().length < 3} onClick={onGenerate}><Sparkles size={17} />{loading ? 'Creating your study set…' : 'Generate study set'}</button></div></section>; }
